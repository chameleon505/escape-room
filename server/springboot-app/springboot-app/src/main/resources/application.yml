server:
  port: 8080


spring:
  datasource:
    url: jdbc:mysql://localhost:3306/rooms_db
    username: root
    password: password
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    show_sql: true
    hibernate:
      ddl-auto: update
      format_sql: true

  servlet:
    multipart:
    max-file-size: 5MB